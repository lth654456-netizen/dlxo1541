# Project Rules

- **Local Model Testing**: If the user includes `@local` in their prompt, you must send the prompt to the local API endpoint at `http://localhost:11434/v1` and use the resulting response to assist with the development.
